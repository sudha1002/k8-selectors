apiVersion: v1
kind: Pod
metadata:
  name: toleration1
  labels:
    purpose: "toleration-demo"
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
  tolerations:
  - key: "hardware"
    operator: "Equal"
    value: "gpu"
    effect: "NoExecute"  